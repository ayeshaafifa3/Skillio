## Frontend environment variables
#
# For local dev, you can omit this entirely because `src/services/api.ts`
# defaults to `/api` (Vite dev proxy -> FastAPI).
#
# For production builds, set this to your backend origin, for example:
# VITE_API_BASE_URL=https://your-backend.example.com
#
# VITE_API_BASE_URL=

